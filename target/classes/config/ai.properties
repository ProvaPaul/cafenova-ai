# Smart Cafe AI Integration Configuration
# -----------------------------------------------
# Set ai.enabled=true only when the Python FastAPI server is running.
# The Java app will fall back to demo data when ai.enabled=false.

ai.enabled=false
ai.api.base-url=http://localhost:8000/api/v1
ai.api.connect-timeout=5
ai.api.read-timeout=30
